# A-09 — deploy, nightly, keepalive

Run 2026-09-07, main at `bcc70ed`. Deploy configuration only; no engine module was edited.

## What ran, and what could not

Four proofs were specified. **One is PASS in the half that can be proven offline. Three are
PENDING on credentials or on another issue landing.** The reasons are recorded below in the same
form A-06 used for its deferred proof, rather than being left absent or claimed from config that
merely looks correct.

### Blockers, in the order they gate the proofs

| # | Blocker | Whose | Gates |
|---|---|---|---|
| B-1 | **Anthropic API credit balance too low** — `400 invalid_request_error: Your credit balance is too low to access the Anthropic API` (`request_id req_011CepuLRSb1FezG93TyhBKF`) | Zaeem | proofs 1b, 2, 3 |
| B-2 | `vercel` CLI not logged in — `vercel whoami` → "No existing credentials found" | Zaeem | proofs 1, 3 |
| B-3 | No GitHub Actions secrets set — `gh secret list` returns nothing | Zaeem | proof 2 |
| B-4 | **A-08's app is not on `main`** — `web/app.py` at `origin/main` is a 57-byte placeholder; the real 15,232-byte app is on `origin/claude/stoic-babbage-5e3238` with no PR open | A-08 | proofs 1, 3 |

B-1 was discovered by running the nightly command sequence for real, not by reading config. It is
the widest of the four: it blocks any code path that calls the model, which includes `almanac
scan`, `/api/lint`, and A-05's eval harness — not only A-09.

## Evidence that did run

**`rates --refresh` — the first nightly step — works end to end.** FMP is reachable and the
credentials are good:

```
cpi_yoy                        3.36  as_of=2026-07-01  via=bls
fed_funds_effective            3.63  as_of=2026-09-03  via=nyfed
mortgage_30y_fixed             6.71  as_of=2026-09-03  via=freddiemac
treasury_10y                   4.78  as_of=2026-09-04  via=treasury
wrote 4 keys to facts/rates.json
```

`facts/rates.json` was restored with `git checkout` afterwards — `facts/**` is protected and A-09
carries no change to it.

**The scan step then failed on B-1**, at `almanac/extract.py:427` in `_call_model`. This is the
step the nightly job runs after the rates refresh, so the nightly workflow will fail at the same
point until credits are restored.

**The corpus fallback selects correctly.** The exact shell from `nightly.yml`, run locally:

```
YT_TOKEN_JSON=(absent)                 -> source=corpus
YT_TOKEN_JSON=(present, real base64)   -> source=youtube
```

and the secret's encoding round-trips: `base64 -i token.json | base64 --decode` reproduced all
746 bytes identically, and the result parses as JSON with keys
`account, client_id, client_secret, expiry, refresh_token, scopes, token, token_uri, universe_domain`.
So the fallback branch — the one that will actually execute while the YouTube quota is exhausted
— is exercised, and the youtube branch's secret handling is proven rather than assumed.

**The Vercel entrypoint imports the real app.** `api/index.py` was loaded with `cwd=/tmp`, to
mimic a runtime that does not root the process at the repository, against a scratch tree carrying
A-08's actual `web/app.py` from its branch:

```
A-08 app.py bytes in scratch tree: 15232
ASGI app type: FastAPI
routes: ['/', '/api/apply', '/api/lint', '/api/report', '/api/scripts', '/docs', ...]
required routes present: True
```

**The `.gitignore` allow-list discriminates.** `git check-ignore` on six paths:

```
TRACKABLE reports/latest.json          IGNORED  reports/latest.md
TRACKABLE reports/2026-09-08.md        IGNORED  reports/2026-09-08.json
TRACKABLE reports/.gitkeep             IGNORED  reports/scratch.json
                                       IGNORED  reports/v1-dump.json
```

Exactly the two files the issue names are trackable. `write_report` also emits `latest.md` and a
dated `.json`; both stay ignored, as does ad-hoc scan output.

## Adversarial probes

Each offline assertion was run against a deliberately wrong artifact and had to fail:

```
keepalive schedule assertion
  real keepalive.yml:              PASS  (crons=['*/10 12-22 8 9 *'])
  ADVERSARIAL schedule deleted:    FAIL  (KeyError)
  ADVERSARIAL hourly cron:         FAIL  (crons=['0 * * * *'])
  -> assertion is meaningful: True

vercel.json maxDuration assertion
  real vercel.json:                PASS  (maxDuration=60)
  ADVERSARIAL maxDuration=10:      FAIL  (maxDuration=10)
  -> assertion is meaningful: True
```

## Proof lines

```
PROOF A-09: live URL GET / 200 and POST /api/lint fresh_wrong.md < 60 s from the deployed host = PENDING (B-2 vercel not logged in; B-4 A-08's app not on main; B-1 API credit balance too low, so /api/lint would 400 even on a live host)
PROOF A-09: workflow_dispatch run of nightly.yml completes green and pushes a commit touching reports/latest.json = PENDING (B-3 no Actions secrets set; B-1 the scan step fails at extract.py:427 on the credit-balance 400, reproduced locally)
PROOF A-09: live /api/report run_at equals the run_at in the pushed reports/latest.json = PENDING (depends on both proofs above; no reports/latest.json has been produced because of B-1)
PROOF A-09: keepalive.yml is valid YAML, scheduled, and one manual run returns 200 = PARTIAL — valid YAML and correctly scheduled = PASS (asserted, with two adversarial probes that fail as required); the 200 half = PENDING (B-2/B-4, no live host to curl)
```

## Closing the pending proofs

`scripts/verify_a09.py --url <deployment>` runs proofs 1, 3 and the live half of 4, each with an
adversarial probe, and prints the proof lines. It requires B-1, B-2 and B-4 cleared first.
