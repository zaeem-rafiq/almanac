# A-05 — blocker: Anthropic credit balance exhausted

**When:** 2026-09-07, during the post-sweep gate sampling (runs 7–12 of a planned 12).

**What was tried.** Six consecutive live `almanac eval` runs on the merged main (post-`d314e9a`)
all passed the gate. A second block of six was started to bring the sample to 12, which would put
the probability of a chance run at ~0.5% rather than the ~7% that 6/6 supports.

**Exact error.**

```
anthropic.BadRequestError: Error code: 400 - {'type': 'error', 'error': {'type':
'invalid_request_error', 'message': 'Your credit balance is too low to access the Anthropic API.
Please go to Plans & Billing to upgrade or purchase credits.'}, 'request_id': 'req_011CepsPUeB1DjnsiuiV1LiY'}
```

**Hypothesis.** Account credit exhausted by this session's live extraction runs — roughly 25 full
eval runs plus the A-05 proof harness. Not a code fault: the same command succeeded minutes
earlier and the failure is a billing 400, not a request error.

**Smallest next step.** Restore credits, then
`venv/bin/python -m almanac eval` six more times and record the pass count in ADR-002 D-5, and
regenerate `evals/results.md`, which is currently from a pre-sweep run.

**Not blocking:** the offline suite (244 passed) and every non-model check run without credits.

## Update — the blocker now also gates the D-2 prompt fix

Credits were still exhausted on re-probe. The D-2 one-claim-per-number prompt change has been
applied and is green offline (246 passed), but the offline suite can only show the prompt says the
right thing, never that the model does it. **D-2 is applied, not verified.**

**Smallest next step, in order:** restore credits → `venv/bin/python -m almanac eval` x6 →
compare against D-5's post-sweep baseline (6/6 gate, stale_material 7/7, matched 45-48) →
regenerate `evals/results.md` → `venv/bin/python scripts/proof_a05.py`.
