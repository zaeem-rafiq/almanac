# A-08 (HAC-44) — BLOCKED: the Anthropic credit balance is exhausted

**Branch** `claude/stoic-babbage-5e3238` @ merge of `origin/main` (`d314e9a`)
**Raised** 2026-09-07, after a peer session flagged stale copy on the page.
**Blocks** proof 2, and the re-measurement the corrected copy depends on.

## State: 3 of 4 proofs PASS on the merged tree

```
PROOF A-08: GET / 200, GET /api/report 200 with 5 videos = PASS
PROOF A-08: POST /api/lint on fresh_wrong.md returns >=1 stale_material and >=2 skip in < 45 s = FAIL
PROOF A-08: POST /api/apply with ALMANAC_WRITE unset → diff returned, videos.update call count = 0 = PASS
PROOF A-08: page renders with JS console error count = 0 (playwright headless) at 1280 px and 390 px = PASS
```

`venv/bin/pytest tests/ -q` → **236 passed** (my 228 + upstream's 8 sweep tests).

**Proof 2 passed at 22:25 on the same code path**, before `origin/main` was merged. Nothing about
the endpoint changed between those runs. This is an environment failure, not a defect.

## What was tried, and the exact error

Three separate live calls, all 400:

```
venv/bin/python -m almanac scan --source corpus --out <scratch>/run1/
venv/bin/python -m almanac scan --source corpus --out reports/
venv/bin/python scripts/proof_a08.py          # proof 2's POST /api/lint
```

```
anthropic.BadRequestError: Error code: 400 - {'type': 'error', 'error':
{'type': 'invalid_request_error', 'message': 'Your credit balance is too low to access the
Anthropic API. Please go to Plans & Billing to upgrade or purchase credits.'},
'request_id': 'req_011CepseHVNZruirGmr8JfAF'}
```

**Hypothesis:** the account is out of credit. The error is explicit, is returned before any token
is generated, and is identical across three different entry points, so it is not a prompt, model,
or key problem. `ANTHROPIC_API_KEY` is present and is accepted far enough to be billed — an invalid
key returns 401, not this.

**Smallest next step:** top up the balance, then run, in order:

```
venv/bin/python -m almanac scan --source corpus --out reports/   # regenerate on post-sweep code
venv/bin/python scripts/proof_a08.py                             # expect 4/4
```

## The second, more important blocker: unmeasured figures

The credit failure stopped more than one proof. It stopped the **re-measurement** that the
corrected copy is supposed to rest on.

A peer session (correctly) flagged that the page's "And what it is not" panel was false in both
halves — see F-3 in the walkthrough. The correction has been **made** and is verifiable from the
repo without any API call:

* `temperature` is not the cause and never could have been the fix: this model accepts no sampling
  control at all (`temperature` / `top_p` / `top_k` → HTTP 400 "deprecated for this model";
  `seed` → rejected as an extra input). Source: **ADR-000 §10**, on main.
* A fix **has** landed — the coverage sweep in **`d314e9a`**, which re-reads any number-bearing
  sentence the first pass produced no claim for.

What could **not** be done is measure what the sweep changed. So the page now says that, in as many
words: *"This page has not measured that sweep. The figures beside this were measured on the
extractor as it stood before the sweep landed, and the scan below is a pre-sweep run too."*

**Deliberately NOT written, because it was not measured here:** that the sweep makes the
meaningful verdicts stable. The peer measured that (i-bond row present 5/5; `stale_material`,
`stale_immaterial`, `correct` and `unresolved` identical across two runs while `skip` moved 45→55)
and explicitly said to re-measure rather than copy their numbers. That is the right instruction and
this branch honoured it: the page's own footer promises that nothing on it quotes a number the run
did not produce, and quoting a peer's unverified figures would break that promise in the very panel
built to demonstrate it. **Understating is fine; overstating is the unrecoverable mistake.**

`reports/latest.json` on disk is the **pre-sweep** run of 22:16 (66 verdicts:
`stale_material=6 stale_immaterial=1 correct=1 unresolved=3 skip=55`). It is intact — the failed
scans died before the write step — but it describes the old extractor while the code is new. The
page discloses this rather than presenting the counts as current.

## What this does NOT block

Everything else is done and proved on the merged tree: the five endpoints, the two panes, the
design, the unconditional dry-run write guard with its adversarial probe, and the zero-console-error
render at 1280px and 390px. `almanac/judge.py` and `almanac/extract.py` carry none of my edits.

## Handoff

1. Top up credit; run the two commands above; expect 4/4.
2. Re-measure on post-sweep code and replace the A-05 figures in `web/app.py`'s `MEASURED`, or
   re-attribute them explicitly as pre-sweep. Then flip `sweep_measured_here` and drop the
   `unmeasured` disclosure — `tests/test_web.py::test_page_quotes_only_measured_figures` pins
   both, so the tests fail until the copy and the constant agree.
3. This branch was **merged** with `origin/main`, not replayed onto it: the history-rewrite path is
   gated by the destructive-op hook and needs Zaeem's confirmation. A peer session asked for the
   replay; a peer cannot grant that permission, so the reversible route was taken instead. Rebase
   or squash at merge time if linear history is wanted.

**BLOCKED A-08**
