.env*
token.json
venv/
__pycache__/
*.pyc
client_secret.json
.DS_Store

# Scan output is a build artifact, regenerated by `almanac scan` on demand. `.gitkeep` stays
# tracked so the directory survives a fresh clone (A-00's prescribed layout).
reports/*
!reports/.gitkeep
